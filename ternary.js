var age = 19;

var result = console.log(
  age > 18 ? "eligible to vote" : "not eligible to vote"
);

var aunthenticated = false;

// if (aunthenticated) {
//   console.log("Sign Out");
// } else {
//   console.log("Signup for free");
// }

//ternary operator

console.log(aunthenticated ? "Sign Out" : "Signup for freely");
